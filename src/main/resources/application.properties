

spring.datasource.url=jdbc:oracle:thin:@//fsktmdbora.upm.edu.my:1521/fsktm
spring.datasource.username=D230405
spring.datasource.password=230405
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# Hibernate/JPA Settings (Auto-creates and updates the 3 required tables)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

# Maps the Railway variable to Spring.
# The text after the colon is a fallback for when you run it locally on your laptop!
# This safely tells Spring to look for an environment variable named CRUD_OPENAI_API
openai.api.key=${CRUD_OPENAI_API}